/** Width of one grid cell in world units. */
export const GRID_CELL_WIDTH = 512;

/** Height of one grid cell in world units. */
export const GRID_CELL_HEIGHT = 256;

/** Extra world padding used for camera framing. */
export const CAMERA_PADDING = 300;

/** Position on the hotel grid (cell indices). */
export type GridPosition = {
	readonly x: number;
	readonly y: number;
};

/** Position in Phaser world coordinates (world units). */
export type WorldPosition = {
	readonly x: number;
	readonly y: number;
};
